import "./master_style.scss";

const AllInOneCard = ({ test }) => {
    switch (test) {
        case "card_default":
            return ""
        default:
            return ""
    }
}

export default AllInOneCard;